export const CUSTOMER_TYPES = Object.freeze({
  LEAD: "lead",
  CUSTOMER: "customer",
});

export const CUSTOMER_TYPE_VALUES = Object.freeze(Object.values(CUSTOMER_TYPES));

export const CUSTOMER_STATUSES = Object.freeze({
  ACTIVE: "active",
  INACTIVE: "inactive",
});

export const CUSTOMER_STATUS_VALUES = Object.freeze(Object.values(CUSTOMER_STATUSES));